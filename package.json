{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/*"
    ]
  },
  "devDependencies": {
    "wsrun": "^2.2.0"
  },
  "scripts": {
    "build": "yarn wsrun build --stages",
    "test": "yarn wsrun test --stages"
  }
}
